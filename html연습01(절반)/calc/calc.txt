calc()
  화면의 값을 구성할 때 가끔 원하는 너비의 값을 남기고 자동적으로 width나 height의 값을 구성할 때가 존재한다. 이러한 경우에 사용한다.
    L> 만약 width: calc(100% - 300px) 이렇게 css의 값을 설정하였다면 화면의 전체 너비에서 300px제외하고 요소의 width를 설정한다.
      L> 이렇게 calc()를 사용할 때 - 사이에 여백을 한 칸씩 주어야 제대로 calc()함수가 동작한다.