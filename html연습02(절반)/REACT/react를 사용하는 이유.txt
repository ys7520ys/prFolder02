리액트를 사용하는 이유
  SinglePageApplication을 만들기 위해서 사용하는 것이 일반적이다.
  L> 새로고침 없이 부드럽게 전환이 가능하다.

  이러한 싱글페이지를 Javascript로도 구현을 할 수는 있지만 코드가 너무 길어지고 복잡해지기 때문에 React 라이브러리를 사용하는 것이다.

  react를 사용하면 html을 재사용하기가 편해진다.

  react를 배우면 react native의 값도 구성할 수 있다.

